<h3>Esqueceu sua senha?</h3>
<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post,:id => "form_login", class: "form" }) do |f| %>
  <%= devise_error_messages! %>
	<%= f.email_field :email, autofocus: true, placeholder: "Email" %>
	<%= button_tag(type: 'submit', class: "btn btn-primary btn-block btn-login") do%>
   Me envie instruções para resetar a senha
  <% end %>
<% end %>
<div class="login-bottom-links">
	<%= render "admin/users/shared/links" %>
</div>
